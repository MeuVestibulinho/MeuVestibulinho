# -------------- App --------------
NODE_ENV=development
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-in-dev

# -------------- Database (Postgres em Docker) --------------
# Porta do host onde o Postgres é exposto (compose mapeia 5433->5432)
DB_PORT=5433
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=meuvestibulinho

# Prisma — ajuste a porta aqui se mudar DB_PORT!
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/meuvestibulinho?schema=public
SHADOW_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/postgres?schema=shadow

# -------------- Keycloak (dev) --------------
# Depois de criar o realm "meuvestibulinho" e o client "nextjs" (Confidential)
KEYCLOAK_ISSUER=http://localhost:8080/realms/meuvestibulinho
KEYCLOAK_CLIENT_ID=nextjs
KEYCLOAK_CLIENT_SECRET=__put_client_secret_here__

# Opcional: porta do container (apenas para o compose)
KEYCLOAK_PORT=8080

# -------------- Providers opcionais --------------
# Discord (se preencher, o provider aparece; se deixar vazio, fica desabilitado)
AUTH_DISCORD_ID=
AUTH_DISCORD_SECRET=

# -------------- PgAdmin (opcional no compose) --------------
# PGADMIN_DEFAULT_EMAIL=admin@local
# PGADMIN_DEFAULT_PASSWORD=admin
# PGADMIN_PORT=5050
